# RewriteEngine On
# RewriteBase /

# RewriteCond %{HTTPS} !=on
# RewriteCond %{HTTP_HOST} ^fastrasuite\.com$ [OR]
# RewriteCond %{HTTP_HOST} ^www\.fastrasuite\.com$
# RewriteRule .* https://fastrasuite.com%{REQUEST_URI} [R=301,L]

# RewriteCond %{HTTPS} =on
# RewriteCond %{HTTP_HOST} ^www\.fastrasuite\.com$
# RewriteRule .* https://fastrasuite.com%{REQUEST_URI} [R=301,L]